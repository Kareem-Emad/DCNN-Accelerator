Initialize your "work" library here:
vlib work
vmap work work/
The src/ and test/ directories will automatically map to it. I can't push the ModelSim _info file because apparently it does not understand relative paths.
